dark:
	mv colors-dark.toml colors.toml
	mv light.mode dark.mode

coffee:
	mv colors-coffee.toml colors.toml
	mv dark.mode light.mode

cannonical:
	mv colors-cannonical.toml colors.toml
	mv dark.mode light.mode

light: coffee
